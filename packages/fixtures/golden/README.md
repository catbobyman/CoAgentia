# golden/ —— 跨语言金标判例（00 §4.4）

指纹（契约 A §2）判例集：Python 权威实现与 TS 镜像必须逐判例一致，CI 双跑。画布图内核（`graph.json`）与拆解校验内核（`decomposition.json`）判例随 DEDAG 重构退役删除，仅存指纹组。

- `fingerprint.json` —— 规范化序列化与 SHA-256 指纹（契约 A §2）。
